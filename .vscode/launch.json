{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Chrome (Direct File)",
            "type": "chrome",
            "request": "launch",
            "url": "${workspaceFolder}/index.html"
        }
    ]
}
